const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			sans: ['Montserrat', 'sans-serif']
		},
		extend: {
			spacing: {
				128: '32rem'
			}
		}
	},

	plugins: []
};

module.exports = config;
